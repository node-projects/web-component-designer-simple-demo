import chalkTemplate from '/web-component-designer-demo/node_modules/chalk-template/index.js'

function chalkFormat (str) {
  if (str) {
    str = str.replace(/`/g, '\\`')
    return chalkTemplate(Object.assign([], { raw: [str] }))
  } else {
    return ''
  }
}

export default chalkFormat
