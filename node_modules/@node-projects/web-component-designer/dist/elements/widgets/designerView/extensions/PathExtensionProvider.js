import { PathExtension } from "./PathExtension.js";
export class PathExtensionProvider {
  shouldExtend(designItem) {
    if (designItem.node instanceof SVGPathElement || designItem.node instanceof SVGRectElement || designItem.node instanceof SVGCircleElement || designItem.node instanceof SVGEllipseElement || designItem.node instanceof SVGLineElement || designItem.node instanceof SVGPolylineElement || designItem.node instanceof SVGPolygonElement) return true;
    return false;
  }

  getExtension(designerView, designItem) {
    return new PathExtension(designerView, designItem);
  }

}