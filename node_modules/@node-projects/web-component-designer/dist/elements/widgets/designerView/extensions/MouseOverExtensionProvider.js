import { MouseOverExtension } from "./MouseOverExtension.js";
export class MouseOverExtensionProvider {
  shouldExtend(designItem) {
    return true;
  }

  getExtension(designerView, designItem) {
    return new MouseOverExtension(designerView, designItem);
  }

}